import '../../imports.dart';

class MyApprovels extends StatelessWidget {
  const MyApprovels({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("My Approvels")),
    );
  }
}
