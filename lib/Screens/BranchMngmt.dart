import '../imports.dart';

class BranchManagment extends StatelessWidget {
  const BranchManagment({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("Branch Management")),
    );;
  }
}
