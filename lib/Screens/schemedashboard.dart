import '../imports.dart';

class schmdashboard extends StatelessWidget {
  const schmdashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("Scheme dashBoard")),
    );
  }
}
