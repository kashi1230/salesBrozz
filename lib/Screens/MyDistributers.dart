import '../imports.dart';

class MyDestributers extends StatelessWidget {
  const MyDestributers({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("My Distributers")),
    );
  }
}
