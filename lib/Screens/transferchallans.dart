import '../imports.dart';

class TransferChallans extends StatelessWidget {
  const TransferChallans({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("Transfer Challans")),
    );
  }
}
