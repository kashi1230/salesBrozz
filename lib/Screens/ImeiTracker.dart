import '../imports.dart';

class Imeitracker extends StatelessWidget {
  const Imeitracker({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("IMEI Tracker")),
    );;
  }
}
