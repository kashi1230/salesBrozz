import '../imports.dart';

class MyEarnings extends StatelessWidget {
  const MyEarnings({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("My earnings")),
    );
  }
}
