import '../imports.dart';

class MyCustomers extends StatelessWidget {
  const MyCustomers({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("My Customers")),
    );;
  }
}
