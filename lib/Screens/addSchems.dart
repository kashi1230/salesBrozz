import '../imports.dart';

class AddScm extends StatelessWidget {
  const AddScm({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("Add Schemes")),
    );
  }
}
