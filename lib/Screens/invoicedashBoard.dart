import '../imports.dart';

class Invoicedashboard extends StatelessWidget {
  const Invoicedashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("Invoice dashBoard")),
    );
  }
}
