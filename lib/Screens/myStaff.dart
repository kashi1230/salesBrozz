import '../imports.dart';

class MyStaff extends StatelessWidget {
  const MyStaff({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text("My Staff")),
    );;
  }
}
